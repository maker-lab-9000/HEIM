Investigate the following alert(s) detected on host "{{ host }}", the Home Assistant VM (Proxmox VM 101; 2 vCPU, 4 GiB) of this homelab. IMPORTANT: you have NO shell access to this host — do NOT use the SSH diagnostic tool (it runs on ubuntu-server, a DIFFERENT machine; only touch it if you need ubuntu-server data for comparison). Work with two tools. (1) 'prometheus_query': the hass_* metrics are the in-guest view — hass_sensor_unit_percent{entity="sensor.processor_use"} (CPU), {entity="sensor.memory_use_percent"} (RAM), {entity="sensor.disk_use_percent"} (disk /), count(hass_entity_available == 0) (unavailable entities), sum(rate(hass_state_change_total[5m])) (recorder DB write rate — a climb means a chatty/flapping entity bloating the DB and growing the disk), rate(hass_automation_triggered_count_total[1h]) (automation activity) — and pve_cpu_usage_ratio / pve_memory_usage_bytes / pve_disk_written_bytes_total with {id="qemu/101"} give the hypervisor view of this VM. Pass lookback (e.g. 24h) to find WHEN a problem started; use 'discover_metrics' with pattern hass_ when unsure of names. (2) 'ha_api': GET /api/error_log FIRST for integration errors; /api/logbook/<ISO ts> around the onset to see what changed; /api/states/<entity_id> for current state and attributes; /api/history/period/<ISO ts>?filter_entity_id=<id> for one entity timeline; /api/config for version and components. Typical root causes: a failing integration (error_log), a flapping or chatty entity bloating the recorder (state_change rate + logbook), a stuck automation, an add-on or a recent update. You cannot restart anything or read files — state clearly what a human should do. Also be rigorous about the guest boundary: physical hardware and other guests are NOT visible from HA data; if evidence points outside this VM, say so. Findings from monitoring:

{{ findings_text }}

{% include 'briefs/_report_spec.md' %}
{% include 'briefs/_temperature_playbook.md.j2' %}
