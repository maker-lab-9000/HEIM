Investigate the following alert(s) detected on host "{{ host }}", which is the Proxmox HYPERVISOR of this homelab. You have THREE vantages: (1) the 'proxmox_api' tool — DIRECT read-only authority on the hypervisor itself: task history incl. vzdump backups/scrubs and their logs (/api2/json/nodes/homelab/tasks?limit=30) — but mdadm RAID scrubs on sdc/sdd are kernel events NOT in the tasks list (use the journal), VM configs (/nodes/homelab/qemu/<vmid>/config), node status, host logs via /nodes/homelab/journal (not /syslog — it times out here), full SMART attributes (/nodes/homelab/disks/smart?disk=/dev/<x>) — but NO physical temperature sensors and NO process list; (2) read-only SSH into the guest VM "ubuntu-server" (qemu/100) ONLY; (3) the Prometheus query tools. There is NO shell on {{ host }}.

DISK TOPOLOGY (verified): homelab nvme0n1 = Proxmox OS + local-lvm thin pool holding ALL VM virtual disks (ubuntu-server root / and /docker-data, home-assistant, ubuntu-sandbox). homelab sda (SanDisk 2TB SSD) holds ONLY ubuntu-server's /Photos (as a qcow2); homelab sdb (480G SSD) holds ONLY /JellyMedia; homelab sdc+sdd (RAID1 md0) and sde are host-only backup disks with NO VM I/O. RULES: I/O on physical sda can come ONLY from ubuntu-server workloads on /Photos (or host-side backup jobs); physical sdb ONLY from /JellyMedia. home-assistant and ubuntu-sandbox CANNOT touch sda/sdb — never attribute sda/sdb activity to them. PVE per-guest disk rates (pve_disk_*) aggregate each VM's disks, which are nvme-backed — they are USELESS for sda/sdb attribution; use homelab node_exporter instead: rate(node_disk_written_bytes_total{instance="${HEIM_PROXMOX_IP}:9100",device="sda"}[5m]) (also _read_bytes_, _io_time_). GUEST device names differ from host: inside ubuntu-server sda=/ (nvme), sdb1=/Photos (host sda), sdc1=/JellyMedia (host sdb), sdd1=/docker-data (nvme) — and cAdvisor device labels are GUEST devices, so container writes on /dev/sda are NVMe/root writes, NOT /Photos.

DETECTED FINDINGS (on the hypervisor):
{{ findings_text }}

INVESTIGATION STRATEGY — follow in order:
1. ATTRIBUTE via Prometheus FIRST (no SSH needed). Use the prometheus_query tool to find which guest is driving the host-level pressure. Useful per-guest series (use pve_guest_info to map ids to names; ubuntu-server is qemu/100):
   - CPU:    pve_cpu_usage_ratio{id=~"qemu/.*"} * 100
   - Memory: 100 * pve_memory_usage_bytes{id=~"qemu/.*"} / pve_memory_size_bytes{id=~"qemu/.*"}
   - Disk IO: rate(pve_disk_written_bytes_total{id=~"qemu/.*"}[5m]) and rate(pve_disk_read_bytes_total{id=~"qemu/.*"}[5m])
   - Pools:  100 * pve_disk_usage_bytes{id=~"storage/.*"} / pve_disk_size_bytes{id=~"storage/.*"}
2. IF the metrics implicate ubuntu-server (qemu/100) as the dominant guest, SSH into ubuntu-server (read-only) and drill into the actual process / mount / workload responsible inside it. For a disk or pool alert, note that ubuntu-server's virtual disk lives on the homelab pool, so its own df / du often explains pool growth directly.
3. IF another guest (e.g. home-assistant, ubuntu-sandbox) or the host itself is implicated, you CANNOT drill into its internals (no SSH; the Proxmox API gives host tasks/logs/configs/SMART but not processes or physical sensors). Report THAT as the conclusion and recommend the operator inspect that guest / the hypervisor directly.

HONESTY CONSTRAINTS (reflect these in the report):
- You are viewing the hypervisor from inside a single guest. This is CORRELATION, not authority — you are blind to other guests, host processes, and physical sensors. Set your confidence accordingly.
- For a TEMPERATURE alert: you cannot read {{ host }}'s physical sensors from inside a guest. The best you can do is correlate a CPU/IO workload spike (via the PVE metrics above) with the temperature rise. Keep confidence LOW unless the correlation is strong, and say so.
- In "## Root cause", explicitly note this guest-side vantage limitation.

{% include 'briefs/_report_spec.md' %}
{% include 'briefs/_temperature_playbook.md.j2' %}
